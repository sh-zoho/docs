---
title: '配置'
description: '使用 `docs.json` 文件自定义您的站点'
icon: 'gear'
---

每个 Mintlify 站点都通过项目根目录中的 `docs.json` 文件进行配置。更改会立即在本地预览中生效。

## 标识

设置站点的名称、主题和视觉标识。

```json docs.json
{
  "name": "Acme Docs",
  "theme": "mint",
  "favicon": "/favicon.svg",
  "logo": {
    "light": "/logo/light.svg",
    "dark": "/logo/dark.svg"
  }
}
```

<ResponseField name="name" type="string" required>
  您的项目名称。显示在浏览器标签页标题和站点元数据中。
</ResponseField>

<ResponseField name="theme" type="string">
  视觉主题。可选项: `mint`、`maple`、`willow`、`sequoia`、`prism`。
</ResponseField>

<ResponseField name="favicon" type="string">
  网站图标的路径。推荐使用 SVG 以在所有尺寸下保持清晰渲染。
</ResponseField>

<ResponseField name="logo" type="object">
  <Expandable title="Logo">
    <ResponseField name="light" type="string">在亮色模式下显示的徽标。</ResponseField>
    <ResponseField name="dark" type="string">在暗色模式下显示的徽标。</ResponseField>
    <ResponseField name="href" type="string" default="/">徽标链接到的 URL。</ResponseField>
  </Expandable>
</ResponseField>

## 颜色

```json docs.json
{
  "colors": {
    "primary": "#16A34A",
    "light": "#07C983",
    "dark": "#15803D"
  }
}
```

<ResponseField name="colors.primary" type="string" required>
  主要品牌色。在亮色模式下用于高亮、激活状态和强调色。
</ResponseField>

<ResponseField name="colors.light" type="string">
  暗色模式的主色。
</ResponseField>

<ResponseField name="colors.dark" type="string">
  用于按钮和重要的交互元素。
</ResponseField>

<ResponseField name="colors.background" type="object">
  覆盖默认的页面背景色。
  <Expandable title="Background">
    <ResponseField name="light" type="string">亮色模式下的背景十六进制颜色。</ResponseField>
    <ResponseField name="dark" type="string">暗色模式下的背景十六进制颜色。</ResponseField>
  </Expandable>
</ResponseField>

## 导航栏

配置顶部导航栏链接和主要号召性操作。

```json docs.json
{
  "navbar": {
    "links": [
      { "label": "Support", "href": "mailto:support@example.com" }
    ],
    "primary": {
      "type": "button",
      "label": "Dashboard",
      "href": "https://dashboard.example.com"
    }
  }
}
```

<ResponseField name="navbar.links" type="array">
  显示在顶部导航栏中的文本链接。
  <Expandable title="Link">
    <ResponseField name="label" type="string">链接的显示文本。</ResponseField>
    <ResponseField name="href" type="string">链接指向的 URL。</ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="navbar.primary" type="object">
  右上角的醒目号召性操作。
  <Expandable title="Primary">
    <ResponseField name="type" type='"button" | "github"'>
      `button` 渲染一个 CTA 按钮。`github` 渲染一个星标数量徽章。
    </ResponseField>
    <ResponseField name="label" type="string">按钮标签。当类型为 `button` 时必填。</ResponseField>
    <ResponseField name="href" type="string">按钮或徽章链接到的 URL。</ResponseField>
  </Expandable>
</ResponseField>

## 导航

将您的页面组织成选项卡和分组。详细信息请参阅[导航](/essentials/navigation)指南。

```json docs.json
{
  "navigation": {
    "tabs": [
      {
        "tab": "Guides",
        "groups": [
          {
            "group": "Getting started",
            "pages": ["index", "quickstart"]
          }
        ]
      }
    ],
    "global": {
      "anchors": [
        {
          "anchor": "Blog",
          "href": "https://example.com/blog",
          "icon": "newspaper"
        }
      ]
    }
  }
}
```

## 上下文工具栏

上下文工具栏浮动在每个页面上,让读者可以一键访问 AI 工具和集成。完整的选项列表请参阅[上下文工具栏](/agent-ready/contextual)。

```json docs.json
{
  "contextual": {
    "options": ["copy", "view", "claude", "mcp", "cursor", "vscode"]
  }
}
```

## 页脚

```json docs.json
{
  "footer": {
    "socials": {
      "x": "https://x.com/yourhandle",
      "github": "https://github.com/yourorg",
      "linkedin": "https://linkedin.com/company/yourco"
    }
  }
}
```

<ResponseField name="footer.socials" type="object">
  显示在页脚的社交链接。支持的键: `x`、`github`、`linkedin`、`discord`、`slack`、`facebook`、`instagram`、`website`。
</ResponseField>
