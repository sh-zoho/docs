---
title: "Markdown 语法"
description: "标准 Markdown 中的文本、标题和样式"
icon: "text-size"
---

## 标题

最适用于章节标题。

```md
## Titles
```

### 副标题

最适用于子章节标题。

```md
### Subtitles
```

<Tip>
  每个**标题**和**副标题**都会创建一个锚点,并显示在右侧的目录中。
</Tip>

## 文本格式化

我们支持大多数 Markdown 格式。只需在文本周围添加 `**`、`_` 或 `~` 即可设置格式。

| 样式 | 写法 | 结果 |
| --- | --- | --- |
| 粗体 | `**bold**` | **bold** |
| 斜体 | `_italic_` | _italic_ |
| 删除线 | `~strikethrough~` | ~~strikethrough~~ |

您可以组合使用这些格式。例如,写 `**_bold and italic_**` 可以得到 **_bold and italic_** 文本。

您需要使用 HTML 来书写上标和下标文本。也就是说,在文本周围添加 `<sup>` 或 `<sub>`。

| 文本大小 | 写法 | 结果 |
| --- | --- | --- |
| 上标 | `<sup>superscript</sup>` | <sup>superscript</sup> |
| 下标 | `<sub>subscript</sub>` | <sub>subscript</sub> |

## 链接到页面

您可以通过将文本包裹在 `[]()` 中来添加链接。您可以写 `[link to google](https://google.com)` 来[链接到 google](https://google.com)。

文档中页面的链接需要是根相对路径。基本上,您应该包含完整的文件夹路径。例如,`[link to text](/writing-content/text)` 链接到我们组件部分中的"Text"页面。

像 `[link to text](../text)` 这样的相对链接打开速度会较慢,因为我们无法对其进行同样高效的优化。

## 引用块

### 单行

要创建引用块,在段落前添加 `>`。

> Dorothy followed her through many of the beautiful rooms in her castle.

```md
> Dorothy followed her through many of the beautiful rooms in her castle.
```

### 多行

> Dorothy followed her through many of the beautiful rooms in her castle.
>
> The Witch bade her clean the pots and kettles and sweep the floor and keep the fire fed with wood.

```md
> Dorothy followed her through many of the beautiful rooms in her castle.
>
> The Witch bade her clean the pots and kettles and sweep the floor and keep the fire fed with wood.
```

### LaTeX

Mintlify 通过 Latex 组件支持 [LaTeX](https://www.latex-project.org)。

```md
<Latex>8 x (vk x H1 - H2) = (0,1)</Latex>
```

TC-07 hotfix branch fix docs: clarify markdown syntax for TC-09 test

Writer test edit for TC-10
